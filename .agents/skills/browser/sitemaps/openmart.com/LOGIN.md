# Openmart Login Reference

- **Profile**: Use `adithya` profile under `profiles/adithya/` (`profiles/adithya/metadata.json`).
- **Vault Reference**: Query `Openmart` in Bitwarden folder `HADES`.
- **Checkpoint Rule**: Stop on captcha or OTP challenge. Authenticated session carries across `openmart.com` and `app.openmart.com`.
