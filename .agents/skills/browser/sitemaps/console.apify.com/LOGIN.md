# Apify Console Login Reference

- **Profile**: Use `apify` profile under `profiles/apify/` (`profiles/apify/metadata.json`).
- **Vault Reference**: Query `Apify` in Bitwarden folder `HADES`.
- **Checkpoint Rule**: Stop on captcha, biometric challenge, or device PIN. Never attempt brute-force password retries.
