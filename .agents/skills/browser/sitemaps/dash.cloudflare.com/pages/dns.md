# Page: Dns (Cloudflare Dashboard)

## Path
`/<account>/<zone>/dns/records`

## Purpose
DNS records management table for A, CNAME, MX, and TXT entries.

## Semantic Anchors
  - **dns_table**: `table[data-testid='dns-records-table']`
  - **add_record**: `button:has-text('Add record')`

## Key Elements
  - **dns_records**: DNS Records (`table`)
  - **add_record_btn**: Add record (`button`)
