# Workflow: Check AutoDS Sync Status (AutoDS)

## Goal
Check AutoDS Sync Status on autods.com using Browser Use CLI 3.0.

## Verified Action Sequence
1. Launch AutoDS dashboard with 'adithya' profile.
2. Check store selector matches target store.
3. Verify monitored products count and alert badges.
4. Extract recent untracked orders if any.
